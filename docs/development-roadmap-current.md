# 小驴打卡当前状态与开发路线

更新时间：2026-09-25

> **2026-09-25 发布快照**：v18.5.0 已发布（GitHub Release/tag）；本版包含叶归 LifeLog 适配器全套、外部协作完善、认知觉醒五件套包、yeguif e2e spec、设置页外部联动分组与移动端折叠修复。全链绿 + 隔离真实内核 E2E 21/21。本文下方 2026-09-23 视角的历史段落保留为背景。

本文是当前版本的路线真值。旧的 `development-roadmap-2026.md`、`development-roadmap.md`、`ui-product-roadmap.md` 和 `ui-redesign-roadmap.md` 保留为历史背景或专项设计记录；其中的版本编号和“规划中”状态不再覆盖本文。

## 新增研究支线（2026-09-22）

[间隔打卡、文档绑定、每日日记、闪卡联动与同类产品调研](roadmap-checkin-research-2026-09.md) 已完成研究归纳，采用项已拆成现有 T-1404～T-1412 等实现批次；剩余项按真实宿主、外部排期或用户决策分别保留开放。后续总方向、生态取舍与实施批次以 [产品战略与生态调研路线](roadmap-product-strategy-2026-09.md) 和 [产品战略落地执行路线](implementation-roadmap-product-strategy-2026-09.md) 为准。

### 思阅 / 思播外部时长联动（T-1382～T-1388）

联动研究和本地适配已交付，详见 [思阅 / 思播与小驴打卡联动可行性研究](roadmap-cross-plugin-study-2026-09.md) 与 T-1383～T-1387。v18.2.1 已包含来源框架、思阅生命周期计时、思播实验采样、健康收件箱和来源设置基础；隔离内核自动化覆盖接线、落盘和幂等，思播仍标为实验且默认关闭。下一步是 T-1386 的断开/重试治理补齐；真实双插件、Android、多窗口和完整播放时序仍归 T-1388。上游公开契约与现场证据未齐前，不宣称默认稳定自动联动。

新增通用协同任务 **T-1395**：对方缺失必要 API 时，采用“本地安全 fallback + 上游公开 API issue/PR”双轨保障。先准备最小版本化契约、能力发现、事件/查询语义、contract fixture、文档示例和旧版降级；上游未合并、未发布或未完成真实宿主验收前，fallback 只能是 opt-in 实验/仅观察路径，不能默认自动写入。思阅、思播与 Task Horizon 的消费端缺口都适用；两条路径指定唯一 canonical source，并用 `source + externalRef` 防止重复累计。

2026-09-22 用户指示升级为**来源统筹框架**（T-1383 范围提升、D-258）：统一「登记→接入→结算→身份→治理」五段管道与四类接入渠道，覆盖各类思源插件与外部应用来源（微信读书、Keep、手机健康中心等）；逐个攻破但框架先行，思阅 MVP 为框架首个租户；外部应用来源先评估后接入（T-1401），见 [外部来源统筹框架](external-source-framework-2026-09.md)。同日 T-1353 摘要驻留经用户批准实现（D-257），外部工具读取验证并入 T-1344 真机窗口。

## 当前状态

| 项目 | 当前结论 |
| --- | --- |
| 发布版本 | **v18.5.0 已发布（GitHub Release，2026-09-25）**；包含叶归 LifeLog 适配器、外部协作完善、认知觉醒五件套包、yeguif e2e spec、设置页外部联动分组与移动端折叠修复，全链绿+e2e 21/21 |
| 思源兼容 | `minAppVersion = 3.8.4`，当前完整验证基线 |
| 数据模型 | 主存储仍为 v3；storeVersion 2 契约不变，无迁移脚本 |
| 外部来源 | 五段框架 + 五渠道：思阅（事件）、思播（采样，实验）、健康收件箱（文档中转）、微信读书（official-pull，T-1402）、叶归 LifeLog（本地读取，T-1457）；上游提案已提交（sireader#55/siplayer#180） |
| 回顾能力 | 趋势/热图/日志/强度/周期对比/成就/提醒/报告导出 + 行动四卡（失速/星期时段/目标负荷/调整有效性）+ 情境×星期交叉 |
| 记录与模板 | 完成/次数/时长/数量/自定义/戒除；65 个模板 + 6 个场景组合包（含认知觉醒五件套） |
| 生态能力 | 公开 API v5、笔记锚点回写、声明式渲染块（含 combo 组合卡片）、Loop/Obsidian 迁移、Dock Tomato 契约 |
| 自动化质量 | 172 守门套件、107 模块架构守门、e2e 真实内核 21/21（自启内核）、宽度走查 49 表面+32 交互 0 失败、i18n parity 1757/1757 |

现阶段产品已经从“补齐功能”进入“真实宿主验证、稳定生态契约和小步迭代”阶段。浏览器和模拟移动 bundle 的结果不能替代真实思源 WebView、触摸、系统键盘、安全区、页签/dock 和双插件时序验证。

## 近期计划：v18.2.x～v18.3.x 小步维护

目标是先完成可以自主推进的契约、纯逻辑、结构测试和文档收口；真实思源桌面、Android、外部插件联调和用户决策项保持开放，不用模拟结果代替现场验收。

### 1. 保留真实宿主验收队列（本轮跳过）

当前只维护 [集成冒烟清单](integration-smoke-checklist.md) 和可自动复现的夹具；以下任务在未来现场窗口再记录设备、思源版本、前端类型、窗口尺寸、截图和结果，本轮不要求用户操作：

- `T-023`、`T-033`：桌面、页签、dock 和移动端残留 UI/重载问题；
- `T-129`、`T-1173`：记录、撤销、筛选、完成态切换后的焦点和滚动；
- `T-1256`：Android WebView 导出、系统保存面板、键盘和回顾浮层；
- `B-007`：Dock Tomato 双插件开始、暂停、继续、完成、放弃、重载、双窗口和移动端联调。

验收证据进入 `PROGRESS.md`；Dock Tomato 现场还必须确认没有数据损坏、重复记账或跨窗口会话误暂停。没有真实宿主证据的场景继续标记为开放，不用浏览器结果替代。

### 2. 保持兼容口径一致

`3.8.4` 继续作为当前声明与自动化验证下限；T-1247/T-1271 已按 D-238 收口，README、清单和兼容矩阵已同步。真实设备证据仍按开放任务维护。若未来要降低下限，必须先完成 3.4.2～3.8.3 的真实回归矩阵。

### 3. 做小范围可观察性和体验修补

- 设置页继续完善番茄钟、Task Horizon 和智能体的状态、错误原因、重试与恢复提示；
- 让事件来源（手动、内置专注、Dock Tomato、API、事项）在回顾和日志中更容易识别；
- 修复真实设备反馈中的布局、触控、键盘和浮层问题；
- 每次改动保持 30 项容量、双主题、无障碍、窄屏和长文本门禁。

近期版本不做云同步、社交排行榜、系统级后台通知或新的万能智能体写入口。

## 自主推进队列与现场验证冻结区
当前不需要用户实际操作即可继续推进的队列：

- 文档真值：同步版本、任务状态、研究结论和执行路线；为每个批次标注 local、host、external、decision 状态。
- 纯逻辑：今日行动台、节奏/逾期率/恢复投影、回顾事实—推断—建议分层、导入预览和来源身份矩阵。
- 契约与结构：API v5 文档、能力协商、错误码、contract fixture、渲染块白名单解析、命令能力矩阵和未知能力 fallback。
- 自动测试：纯函数、迁移/导入导出、失败回滚、确定性、性能基线、ARIA/键盘/窄宽度和双主题结构门禁。
- 发布准备：构建、类型检查、完整质量链、资产清单、回滚演练和变更文档。

以下事项本轮明确冻结，不由自动化结果替代：

- 思源桌面、页签、dock 和 Android WebView 的触摸、键盘、安全区、重载和真实文档读写。
- 思阅、思播、健康、Task Horizon、Dock Tomato 等外部插件的真实联调和时序证据。
- 快捷入口截图 backlog 的最终视觉取舍；在截图齐全且收到开始信号前只维护计划，不改该工作流代码。
- 微信读书 Key、API 条款/限流、积分兑换、自建同步和任何需要用户隐私或产品取舍的决定。
- 向上游维护者发 issue/PR、推送、发版和需要外部账号的操作。

自主队列完成后只记录为“自动化证据已通过，现场验证仍开放”，不关闭对应真实宿主任务。

## v18.x 已交付基线与开放协作项

> v18～v22 文档保留为已交付基线与历史规划快照；下一阶段的产品方向和实施批次以 [roadmap-product-strategy-2026-09.md](roadmap-product-strategy-2026-09.md) 与 [implementation-roadmap-product-strategy-2026-09.md](implementation-roadmap-product-strategy-2026-09.md) 为真值。

本段保留 v18.x 的交付基础和未完成的外部协作项；本地新开发不再按此段重复立项，下一阶段以产品战略和落地执行路线为准。

### 1. Task Horizon 公开契约联调

完成 `T-1165`、`T-1228`～`T-1230`，只使用双方已记录的公开 API：

- 日历「打卡」图层读取有界摘要或范围事件；
- 原生任务复选框真实完成后写入 `recordEvent`；
- 使用 `taskhorizon:<blockId>:<localDate>` 幂等身份，重复回放不重复记账；
- 双方互置契约测试和刷新事件测试。

日历可见性规划（T-1389～T-1394）的小驴侧基础已交付：T-1390 项目级 `taskHorizonCalendarVisible`（缺省 true）、T-1391 API v5 `calendar.read`/`getCalendarProjection` 有界项目×日期只读投影、T-1393 编辑器显示开关已完成。隐藏只影响 Task Horizon「打卡」日历图层，不删除小驴事件、不复用归档、不默认关闭任务完成回写；旧 v1 聚合接口不能保证项目级隐藏。T-1392 消费端实现和 T-1394 双向现场验收仍开放，详见 [日历可见性规划](roadmap-task-horizon-calendar-visibility-2026-09.md)。

Task Horizon 消费端如果缺少 `calendar.read`、投影刷新或降级入口，纳入 T-1395 的上游 API/bridge PR 协同；当前只准备本地契约草案和夹具，不修改 `task-horizon-v1.json`，不把私有实现当作稳定依赖。

对方没有排期时，本插件继续维护契约夹具、诊断和降级路径，不把外部等待变成版本阻塞。

### 2. API v5 稳定化

把已实现的范围读取、统一项目查询、批量写入和指标门面整理为稳定文档：类型定义、最小示例、错误码、限制、兼容矩阵和契约测试包。v4 能力保持兼容，新增能力继续采用能力发现和有界输入。

### 3. 回顾与导出增强

在现有趋势、周期对比和日志基础上，完善可配置周报/月报、来源筛选、目标偏差解释和安全导出。所有指标必须能够回溯到原始事件、项目修订和统计截止时间，并覆盖空数据、小数据、大数据、跨时区和夏令时边界。

### 4. UI 进入维护阶段

重点从“增加页面”转为降低维护成本：清理重复 token 和历史样式、复用反馈/空态/错误/保存状态组件、减少布局跳动，并持续关注生产 CSS 体积。每轮仍运行完整质量链，不能以体积优化换取触控、可读性或数据流程回归。

同时明确两套版本语义：内部 `CheckinStore` 已为 v3，公开 API descriptor 的 `storeVersion: 2` 是公共契约版本。后续若调整任一数字，必须先补决策记录、迁移说明和契约测试，避免第三方误把私有存储版本当成公开 API 版本。

### 5. 恢复与并发演练

补齐真实升级/降级、损坏数据隔离、双窗口并发、断网后重试和恢复点演练，形成用户可读的诊断与恢复指南。

## 长期方向：按能力里程碑推进

长期方向是开放成熟能力，同时维持本地优先、可解释和可撤销的产品边界。

### 1. 开放生态

- 稳定 API v5 的类型、版本协商、错误码和弃用周期；
- 发布适配器准入清单和契约测试包，要求身份、幂等、卸载清理、权限和失败隔离全部通过；
- 至少有两个真实消费方后，再扩大新的写入能力；
- 形成可重复的预发布、回滚、资产摘要和升级/降级流程。

### 2. 笔记原生差异化

继续深化笔记锚点、日记回写和声明式渲染块：支持可配置数据集、热图/汇总表达式和日期跳转。所有回写默认关闭、用户主动开启，保留审计、失败隔离和撤销能力，插件私有存储继续作为事实来源。

### 3. 受控智能体

维持“只读 → 建议 → 用户确认执行”三层边界。执行前显示差异和影响范围，写入经过审计、冲突检查、错误码、重试和撤销；未配置模型时，手动打卡、统计、导出和恢复完整可用，不提供万能写入口。

### 4. 习惯内核二期

SKIP、强度、弹性周期、at-most 和成熟度模型已经落地，后续根据真实反馈优化周期目标、里程碑、反内疚文案和统计口径。暂不引入 RPG、社交压力或自建同步，除非用户需求和隐私模型都明确支持。

### 5. 性能与隐私

维护 10k/100k 事件、低端设备、跨窗口和多插件组合基线；评估端到端加密或外部同步时，先完成隐私、冲突、撤销和数据所有权设计，再决定是否立项。

### 6. 生态调研循环（常设，2026-09-22 起）

常设调研循环（T-1400、D-256）按里程碑窗口推进：每轮对思源集市、Obsidian/Logseq 等笔记插件生态、独立习惯应用与 GitHub 同类项目做增量调研，按「扫描 → 候选清单 → 评估卡 → 吸收 1～3 项开发 → 自动验证 → 复盘」推进。发布单独检查授权和现场证据，不作为下一轮本地调研的前置。既定实现批次优先，现场/外部等待不阻塞自主队列；具体映射见实施路线 M0～M5。


## 版本节奏与决策门

| 阶段 | 能力里程碑 | 本地进入条件 | 发布时另行核验 |
| --- | --- | --- | --- |
| 近期 | M0～M1：基线、日期、今日、入口与引导 | 当前源码与范围明确，自动夹具可复现 | 相关现场清单、完整质量链、升级/回滚说明 |
| 中期 | M2～M3：可解释统计、笔记与迁移 | 日期/筛选契约稳定，已有数据和写入路径可复用 | 契约测试、性能基线、文档写入与恢复证据 |
| 长期 | M4～M5：生态、治理与质量 | 能力协商和降级明确，纯投影/控制面可自动验证 | 受影响的真实消费方联调、隐私边界、兼容/弃用与撤销证据 |

未来版本号在范围与发布窗口冻结后确定；真实消费方数量只约束扩大生态写入能力，不阻塞无关本地功能。质量、无障碍和隐私边界贯穿每个里程碑，M5 负责集中补齐跨模块缺口。

每个版本都保留以下固定门槛：

- `pnpm run check`、`pnpm run build`、`pnpm run test:quality`、`pnpm run check:release`；
- 浅色/深色、桌面/页签/dock/移动端、30 项容量、长名称和长单位；
- 键盘焦点、ARIA、触控尺寸、软键盘、安全区和导出路径；
- 真实宿主证据与自动化证据分开记录；
- 版本、提交、构建产物和 SHA-256 保持一致。

## 当前风险

1. 真实宿主和双插件证据不足，可能遗漏 WebView、系统键盘、安全区和事件时序问题。
2. Task Horizon、Dock Tomato 等外部排期不可控，必须保持本地降级路径和独立版本节奏。
3. 提高 `minAppVersion` 会让旧思源自动禁用插件，任何下限变更都要在发布说明中明确影响。
4. 笔记回写、渲染块和智能体扩大写入面，必须坚持 opt-in、公开 API、审计、失败隔离和可撤销。
5. 旧路线文件较多，后续新任务统一登记到本文、TODO、PROGRESS、BLOCKERS、DECISIONS，避免出现多个“当前路线”。
