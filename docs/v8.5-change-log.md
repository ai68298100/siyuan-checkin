# 8.5 变更记录

## 新增

- **JSON 导入/恢复（设置 → 数据与导出）**：用之前导出的 JSON 文件整体恢复打卡数据。导入前确认框显示项目数与记录数，恢复操作不可逆（覆盖当前数据），确保用户知情。

## 兼容性

- 需要思源 v3.4.2+；无数据结构变更。

## 数据安全闭环

至此 导出 ↔ 导入 循环完整：CSV 导入（增量）+ JSON 导入（全量恢复）+ JSON/CSV 导出。
