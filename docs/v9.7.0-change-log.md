# 9.7.0 变更记录

日期：2026-09-14

主题：提醒与智能体协作收口，GitHub Release 已发布；思源集市审核暂缓。

## 本版本亮点

- **提醒中心**：逾期提醒成为独立状态，支持历史逾期补记；延期、跳过和恢复动作拥有独立持久化、跨日过期规则、状态迁移守卫与有界审计日志。
- **移动端体验**：修复真实设备截图反馈中的窄屏布局问题，收敛顶栏高度与卡片操作区；桌面页签、dock 与移动端共享容器响应式布局，减少横向溢出。
- **今日页与回顾页**：增加最高优先级提醒入口、离线本地摘要、分析历史缓存与只读版本对比；局部刷新在结构稳定时保留焦点、滚动和窗口级提示。
- **智能体建议安全闭环**：结构化建议经过字段白名单和差异预览，确认/取消/撤销状态持久化并带审计、过期清理、并发保护和恢复逻辑；对外仅开放 `suggestions.read` 只读能力。
- **生态事件契约**：建议工作流更新事件具备类型守卫、状态枚举校验和防御性克隆，第三方可安全订阅状态变化但不能获得静默写入权限。
- **发布工程**：CI 增加浏览器无障碍与视觉审计；发布资源检查包含 CSS 体积预算，构建产物版本保持一致。

## 验证

- `pnpm run test:quality` 全链通过（环境、类型、主测试、UI、移动端、生态、性能、发布资源、生产构建）。
- 系统 Chrome 双主题无障碍审计、视觉走查和宽度走查通过。
- `package.zip` SHA-256：`17DA1F5192D9046855618D517E18E12768E41006E19C8235245B1504B70A6E14`。

## 已知限制

- T-023/T-129 真实思源客户端验收仍待用户在桌面端、页签、dock 和手机端完成；自动化测试不能替代该证据。
- 本版本暂不提交思源集市审核，建议通过 GitHub Release 手动安装。
