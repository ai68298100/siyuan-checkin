# 17.2.0 变更记录

## 发布后本地 UI 修订（2026-09-20，未发布）

- 第二批覆盖七页全端：窄屏少量项目也紧凑，宽屏多项三列；顶栏全宽消除两角白块；回顾建议/事项长备注/番茄收件箱渐进展开；编辑保存栏可达、归档选择及触控修复。维持新设计语言，不修改打卡存储。
- 响应验收扩为每组42基础+16长内容，覆盖桌面弹窗/页签/侧栏、手机双色及短横屏；30项密度、顶栏边缘、保存命中等采用真实构建浏览器断言。

- 桌面顶栏重排为品牌、胶囊导航与弹窗控制；今日页新增完成概览、真实连续记录和快捷专注入口。
- 统一浅色/深色卡片、编辑器和维护页风格；保留两项 18px 原生复选框修复。
- 超过 12 项自动进入紧凑排列，面向 20–30 项使用场景：宽屏双列、窄屏单列，保留名称/进度/主要操作，精简重复信息与概览占用。
- 名称直达编辑，右键/长按菜单支持复盘；快速记录与撤销同步更新数值和概览。
- 此本地修订还包含当天完整目标时长联动（T-1305），未修改已发布的 GitHub v17.2.0 资产。

17.2.0 是联动与生态版本：完成底栏番茄钟 PR #5 评审的全部消费端修复（五轮核查）、公开 API 升级到 v5、新增 Obsidian Habit Tracker 21 迁入通道，并把最低思源版本校准到实测基线 3.8.4。**主存储结构不变（仍为 v3），可直接升级，无需迁移。**

## 底栏番茄钟联动修复（D-235~D-237/T-1266~T-1270）

- 专注启动不再「成功即暂停」：启动后只校验业务前提（专注专用指纹 = 日期修订 + direction + tomatoMode），不再复检 `canStart`。
- 跨插件会话归属：`focus.start()` 必须返回非空 sessionId 才接管（否则 `DOCK_TOMATO_START_UNCONFIRMED`，不猜测不暂停）；停止经 provider/失效/active/sessionId/阶段五重守门，`pause-session` 能力存在时原子按会话暂停；休息阶段共享父 ID 不误暂停。
- 生命周期解绑：`available:false` 即时解绑并失效该 facade；注销/替换/卸载均为纯解绑（`{stopActive:false}`），卸载不再暂停外部计时器；完成清理立即释放「正在专注」，移除 5 秒全局空闲轮询。
- 完成回写持久收件箱 `checkin-docktomato-inbox`：先缓冲再处理（未就绪/刷新失败不丢通知），判定顺序 duplicate → user-removed → blocked → 写入；`completedAt` 为记账时刻（缺失/无效拒绝，不回退当前时间）；1s/5s/30s 幂等重试；容量 200 满员拒绝。
- 收件箱手动管理（设置页）：立即重试、丢弃、以及 skipped-day 条目的「撤销跳过并计入」——同受保护单元内 skip 墓碑与完成记录一起落库、一起回滚。
- 戒除类（`atMost`）目标禁用专注入口并说明原因；番茄来源备注随插件语言本地化。

## 渲染块文档/笔记本维度与稳定性（T-1292/T-1293）

- ```checkin``` 配置新增 `docId`/`notebook`：只统计锚点块位于该文档/笔记本的项目；作用域优先级 itemIds > group > docId/notebook > 全部活跃;无索引/未命中 fail-closed 返回空,不静默放大范围。
- 锚点归属经宿主 `/api/block/getBlockInfo`（`rootID`/`box`,会话内缓存）解析;索引未命中先出加载占位,解析完成后强制重渲染一次;不可解析锚点（如块被删除）只请求一次,不循环。
- 修复相邻渲染块互删预览的真实缺陷:预览按源块归属标记（`data-checkin-preview-for`）管理,只删属于自己的旧预览。

## 性能与验证增强（T-1288/T-1285/T-1289/T-1293）

- 10 万级事件性能门禁:范围事件读、200 条批量规划、`computeLongestStreaks` 全历史扫描各设灾难退化捕获。
- 真实内核 E2E 扩至 15 项:新增批量写落盘/重载幂等、渲染块 docId 作用域与 fail-closed、跳过日解析旅程（设置页→撤销跳过并计入→同单元落库断言）、移动端番茄联动。

## 公开 API v5（D-240/T-1275~T-1278/T-1280）

- 协议版本 4→5，能力清单 14→18：新增 `items.query`（统一项目投影,归档语义二选一）、`events.range.read`（有界半开区间事件读,`truncated` 截断标注）、`events.record.batch`（幂等批量写,单次持久化,结果 1:1: recorded/duplicate/discarded/blocked/rejected,单批 200）、`metrics.read`（`getStreaks` 返回 current+longest）。
- `describe()` 新增 `capabilitiesSince`：每项能力首次出现的协议版本,v4 消费方在 v5 宿主上可逐能力探测。
- v4 面冻结兼容：单条 `recordEvent` 行为不变；Task Horizon 契约 `minApiVersion` 钉 4 不受影响。

## Obsidian 迁入通道（T-1279）

- 设置页「从 Obsidian 导入」：解析 Habit Tracker 21 的习惯 .md frontmatter（title/color/maxGap/entries,引号、块列表与内联列表变体,非法日期消毒）；一习惯一文件映射为每日二值项目 + `source:"import"` 打卡；幂等身份 `obsidian21:<文件名>:<日期>`（前缀已登记）。颜色与 maxGap 容忍不迁移,确认文案如实说明。
- 迁出通道（T-1283）：每个活跃项目导出为一个 H21 习惯 `.md`（frontmatter title + entries 完成日数组,升序）；完成日 = 非跳过事件日；文件名消毒 + 冲突唯一化；无完成日/超上限（30）项目计入跳过并如实提示；round-trip 测试锁定导出→解析无损还原。

## 最低版本与预算

- `minAppVersion` 3.4.2→**3.8.4**（D-238）：原声明值从未在该版本回归,而渲染块 DOM 与智能体能力的实测依赖在 3.8.x;低于 3.8.4 的思源会禁用本插件,请先升级思源。
- CSS 分级预算放宽为 480KB 告警 / 520KB 硬阻断（D-239,用户授权）；318KB 历史软线保留。

## 质量与验证

- 新增测试文件：`focus-adapter`（启动误暂停复现）、`docktomato-inbox`（收件箱纯函数）、`api-v5`（范围读/批量规划/longest）、`obsidian-habits`（解析变体）;清单 133 个文件、0 退役。
- 真实思源 3.8.4 内核 E2E:`test:e2e` 8/8 + `test:e2e:readonly` 1/1（覆盖卸载纯解绑、双窗口、移动端、建议执行撤销）。
- 浏览器级验收：宽度走查 12/12 无溢出;双主题 visual-qa 各 277 项检查符合预期。
- 完整 `test:quality` 全链 exit 0;生产 CSS 448,170 bytes。
