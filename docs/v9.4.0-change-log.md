# 9.4.0 变更记录

日期：2026-09-12

主题：T-022 架构重构——主文件拆分完成（index.ts 4684 → 1853 行）。

## 重构（无行为变更）

index.ts 由单一 4684 行文件拆为 **21 个模块**，全部保持原有行为与用户界面逐字不变：

**页面视图（src/render/）**
- `fragments.ts`：今日页碎片（事项横幅/保存状态/同步提示/项目卡/日志/近期事项）
- `review.ts`、`occasions.ts`、`settings.ts`、`editor.ts`：回顾/事项/设置/编辑器整页视图

**事件绑定（src/render/）**
- `bind-today.ts`、`bind-occasions.ts`、`bind-page-navigation.ts`、`bind-editor.ts`（原 500 行单体）
- `today-bindings.ts`（快捷数字键 / 批量模式 / 拖拽排序）
- `quick-dialog.ts`（弹窗生命周期、尺寸偏好、速切快捷动作）
- `focus-timer.ts`、`focus-adapter.ts`、`save-form.ts`

**服务与工具**
- `api.ts`：对外集成 API 工厂（CheckinApi/CheckinApiHost）
- `agent-capabilities.ts`：9 个思源智能体能力注册
- `navigation.ts`：页面导航
- `plugin-ops.ts`：后台渲染、月份切换、项目恢复、导出、CSV 导入等零散操作
- `model-helpers.ts`：克隆/指纹/连续天数/事件构造等纯函数
- `shared.ts`、`ui/icons.ts`、`ui/labels.ts`、`version.ts`

**拆分手法**：类内保留同签名薄壳（`this as unknown as XHost` 结构转换），运行时零开销；测试中的源码断言同步指向对应模块。

## 验证

- build / tsc / test / test:ui / test:mobile / test:ecosystem / check:release 全链通过
- 双主题视觉走查、宽度走查（330–2000px）、无障碍审计（0 违规）通过
- 性能基准：10k 记录全量渲染 21ms（阈值 3s）

## 遗留

- T-023 真实设备验证修复（阻塞于用户测试）
