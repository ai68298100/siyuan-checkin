# 小飞驴打卡 v13.0.1

这是 v13.0.0 的紧急修复版本：修复编辑器新建条目保存被写后校验误判失败并回滚的缺陷，v13.0.0 用户请尽快升级。

## 更新内容

- 修复新建条目保存失败：写后回读校验对"缺省 archived 字段"与"物化为 false"误判为并发冲突，导致保存重试后回滚；现插件侧条目构造与规范化字段集合严格一致。
- 浏览器质量走查宿主按存储名分槽模拟、克隆失败显式拒绝，避免伪失败掩盖真实缺陷。
- 同步模板目标值步长期望（T-1091 步长分离），双击提交步骤等待卡片渲染，消除竞态误报。

## 安装

下载本 Release 的 `package.zip`，在思源中通过"设置 → 集市 → 下载安装包"安装。升级前建议在插件设置页导出 JSON 备份。

## 验证说明

自动化测试覆盖类型、构建、主功能、UI、移动端、生态、性能和发布资源，并通过浏览器视觉走查全链（真实宿主行为仍建议现场复核）。

## 产物校验

- 文件：`package.zip`
- 版本：`v13.0.1`
- SHA-256：`6f737afcc1a3fa07081eea4b6383d82c745c3d32c807a4ca361ab2e7b8facc3c`
