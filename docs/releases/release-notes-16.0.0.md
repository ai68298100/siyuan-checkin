# 小飞驴打卡 v16.0.0

这是复盘基线比较版本：支持把当前日期范围与同跨度前置范围进行结构化对比，并为后续趋势分析提供稳定、纯函数的数据模型。

## 更新内容

- **复盘范围比较**：输出总事件、完成项目、计划项目的当前值、基线值和 delta。
- **逐项目差值**：按项目并集比较，缺失项目补零，结果可直接用于图表、导出和洞察。
- **前置范围推导**：根据当前本地日期范围生成同跨度前置范围，严格拒绝非法日期和逆序范围。
- **Task Horizon 稳定性**：延续公开 bridge 的协议探测、重试、并发单飞、停止竞态和大规模 replay/stress 矩阵。
- **发布质量**：范围比较测试已接入统一质量门禁，完整链路覆盖类型、构建、功能、生态、性能与发布资源。

## 验证说明

自动化质量链通过不替代真实思源宿主中的桌面、页签、dock、移动软键盘、多窗口及 Task Horizon 双方联调验收。

## 安装

下载本 Release 的 `package.zip`，在思源中通过“设置 → 集市 → 下载安装包”安装。升级前建议导出 JSON 备份。

## 产物校验

- 文件：`package.zip`
- 版本：`v16.0.0`
- SHA-256：`cf69005ae5ff52e99680893149897f69b48e79830f66a84d564719c194a62492`
