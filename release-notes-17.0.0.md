# 小飞驴打卡 v17.0.0

这是习惯体系融合版本：跳过态、习惯强度、负向目标、笔记回写与声明式渲染块，六个批次二十三项任务的整合发布。数据存储在加载时自动从 v2 迁移到 v3，无需手动操作。

## 更新内容

### 跳过与宽容（核心新增）

- **跳过态**：Today 卡片菜单（右键/长按）可「跳过今日」，可填原因、可撤销；批量模式支持跳过。跳过日不断连击、不计入完成率分母、不吃配额，热力图与月历以中性色呈现。
- **宽容提醒**：已完成的打卡不再出现在待办提醒中；跳过日不再提醒；超期 snooze 自动清理。
- **反内疚文案**：连续中断后显示「已重新开始」，跳过和中断不会清掉历史记录。

### 习惯强度与复盘

- **强度分数**：每项目 0~100 的习惯强度（半衰期模型），回顾页新增强度趋势折叠卡。
- **较上一周期**：回顾页新增范围对比——记录、完成、安排的增减与逐项目差值。
- **成熟度**：洞察页新增 sigmoid 成熟度百分比（66 天参考线）。
- **超额徽章**：数值型单日完成量达目标 150% 解锁「超额一天 / 十次超额」。
- **教练建议**：连续跳过自动建议下调频率；强度下滑提示恢复最小节奏（只建议、不自动改）。

### 数据迁移与所有权

- **Loop Habit Tracker 迁移**：导入官方导出的 Habits.csv / Checkmarks.csv；导出 Loop 兼容双文件。
- **导出格式总览**：JSON 备份、CSV 记录、Markdown 报告、Loop 迁出四条通道齐备，见 `docs/export-formats.md`。

### 负向目标（戒除类）

- **戒除类目标**：编辑器勾选「戒除类目标」——不记录即成功，记录即破戒；连击 = 连续无破戒日。
- **戒除模板包**：戒烟、戒奶茶、限制咖啡、不熬夜刷手机、戒糖饮料。

### 笔记联动（opt-in）

- **笔记锚点**：项目可绑定思源块/文档，打卡状态回写为其自定义属性（`custom-lv-checkin`），可搜索可复核；失败自动挂起并入审计，不阻断打卡。
- **打卡即笔记**：打卡备注/跳过原因可追加为锚点文档的子块（带日期戳）；解绑或卸载插件时清除插件写入的属性。

### 文档内渲染块

- **```checkin 渲染块**：在文档代码块中写 JSON 配置，即可渲染月历 / 热力 / 汇总视图（支持项目、分组、全部三种作用域与色阶配置）；点击日期跳转回顾页当日详情；打卡后自动刷新。

### 周报与报告

- **周报/月报模板**：记录条数、完成概览、项目明细、上一周期对比、亮点说明五区块可配置，基线自动对比；一键导出 Markdown。

## 存储迁移说明

- 存储版本 2 → 3：新增事件可选 `kind` 字段（跳过态）。加载时自动迁移，历史数据无损；导出的 JSON 备份可被旧版本读取（旧版本会忽略新字段）。
- 建议升级前照例导出 JSON 备份。

## 验证说明

完整质量链（类型、构建、功能、UI、移动、生态、扩展、对比、性能、发布资源）通过；浏览器宽度走查与双主题视觉检查通过。自动化不替代真实思源宿主中的桌面、页签、dock、移动软键盘与多窗口验收。

## 安装

下载本 Release 的 `package.zip`，在思源中通过“设置 → 集市 → 下载安装包”安装。升级前建议导出 JSON 备份。

## 产物校验

- 文件：`package.zip`
- 版本：`v17.0.0`
- SHA-256：`4ccb756719f115e933d1d52a954e6353aa17c67f428e3874a40f58a316a0ba4b`
